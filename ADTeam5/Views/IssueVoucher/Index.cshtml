
@model List<ADTeam5.ViewModels.TempVoucherDetails>

@{
    ViewData["Title"] = "Index";
}

<h1>Items to be added to voucher</h1>


<div class="container">
    <div class="row">
        <div class="col">
            <div id="TempVoucherDetailsList">
                <partial name="_TempVoucherDetailsList" model="Model" />
            </div>
        </div>
    </div>
</div>

<!-- Core -->
@*<script src="/assets/vendor/jquery/jquery.min.js"></script>*@
@*<script src="/assets/vendor/popper/popper.min.js"></script>*@
@*<script src="/assets/vendor/bootstrap/bootstrap.min.js"></script>*@
<script src="/assets/js/jquery.unobtrusive-ajax.min.js"></script>




@*<div class="container">
        <div class="row">
            <div class="col-12">
                @if (Model.Count() == 0)
                {
                    <div class="row justify-content-center text-center">
                        <div class="col-12 col-md-10 mt-5">

                            <i class="fa fa-user fa-5x mb-5"></i>

                            <h2>
                                No Record Found
                            </h2>

                            <p class="text-muted">
                                You do not have any items to be added to voucher.
                            </p>
                            <a class="btn btn-lg btn-primary d-none d-md-inline-block mt-3" asp-action="Create">Create New</a>
                        </div>
                    </div>  / .row
                }
                else
                {
                    <table id="TempVoucherDetails" class="table table-striped table-bordered" style="width: 100%;">
                        <thead>
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.ItemNumber)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.ItemName)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Quantity)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Remark)
                                </th>
                                <th>
                                    Action
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.ItemNumber)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.ItemName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Quantity)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Remark)
                                    </td>
                                    <td>
                                        <a asp-action="Edit" asp-route-id="@item.ItemNumber">Edit</a>
                                    </td>
                                    <td>
                                        <form asp-action="VoucherItemDelete" asp-route-id="@item.ItemNumber" data-ajax="true" data-ajax-update="#TempVoucherDetailsList">
                                            <button type="submit" class="btn btn-sm btn-danger d-none d-md-inline-block">
                                                Delete
                                            </button>
                                        </form>
                                    </td>
                                    <td>
                                        <input name="itemNumber" type="checkbox" value="item.ItemNumber">

                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <a class="btn btn-lg btn-primary d-none d-md-inline-block mt-3" asp-action="Create">Create New</a>
                    <input type="submit" value="Add to Voucher" class="btn btn-primary" />
                }
            </div>
        </div>
    </div>*@

