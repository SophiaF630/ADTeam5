@model List<ADTeam5.ViewModels.TempPurchaseOrderDetails>

@{
    ViewData["Title"] = "_TempPurchaseOrderDetailsList";
}

<script src="~/lib/jquery/dist/jquery.min.js"></script>
<script src="~/lib/bootstrap/dist/js/bootstrap.min.js"></script>



<div class="container">
    <div class="row">
        <div class="col-12">
            @if (Model.Count == 0)
            {
                <div class="row justify-content-center text-center">
                    <div class="col-12 col-md-10 mt-5">

                        <i class="fa fa-user fa-5x mb-5"></i>

                        <h2>
                            No Record Found
                        </h2>

                        <p class="text-muted">
                            You do not have any items to be added to PO.
                        </p>
                        <button type="button" class="btn btn-lg btn-primary d-none d-md-inline-block mt-3 createNewPOItem" style="width:120px;height:40px;border:2px; background-color:#3c8dbc">
                            Create New
                        </button>
                    </div>
                </div> <!-- / .row -->
            }
            else
            {
                <button type="button" class="btn btn-primary createNewPOItem" style="width:100px;height:32px;border:2px; background-color:#3c8dbc">
                    Create New
                </button>

                <table id="TempPurchaseOrderDetails" class="table table-striped table-bordered" style="width: 100%;">
                    <thead>
                        <tr>
                            <th>
                                No.
                            </th>
                            <th>
                                Item Number
                            </th>
                            <th>
                                Item Name
                            </th>
                            <th>
                                Quantity
                            </th>
                            <th>
                                Remark
                            </th>
                            <th></th>
                            <th></th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="RowID">
                                    @Html.DisplayFor(modelItem => item.RowID)
                                </td>
                                <td class="ItemNumber">
                                    @Html.DisplayFor(modelItem => item.ItemNumber)
                                </td>
                                <td class="ItemName">
                                    @Html.DisplayFor(modelItem => item.ItemName)
                                </td>
                                <td class="Quantity">
                                    @Html.DisplayFor(modelItem => item.Quantity)
                                </td>
                                <td class="Remark">
                                    @Html.DisplayFor(modelItem => item.Remark)
                                </td>
                                <td>
                                    <button type="button" class="btn btn-primary editPOItem">
                                        Edit
                                    </button>
                                </td>
                                <td>
                                    <form asp-action="POItemDelete" asp-route-id="@item.RowID" method="post" data-ajax="true" data-ajax-update="#TempPurchaseOrderDetailsList" role="form" asp-controller="IssuePO">
                                        <button type="submit" class="btn btn-sm btn-danger d-none d-md-inline-block">
                                            Delete
                                        </button>
                                    </form>
                                </td>
                                <td>
                                    <input type="checkbox" id="isChecked" name="isChecked" value="@item.RowID">
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
                <label>Total Amount:</label>
                <br />
                <input type="submit" value="Add to Draft" class="btn btn-primary addToDraft" />
                <input type="submit" value="Submit" class="btn btn-primary submit" />
            }
        </div>
    </div>
</div>


<!-- Modal for create PO item-->
<div class="modal fade in" id="createNewPOItemModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <br />
            @*<div class="modal-header">*@
            <div align="center" vertical-align="middle">
                <div style="width: 400px;padding: 15px;margin: 15px;border-style:outset; border-width:1px; ">
                    <div align="center" vertical-align="middle">
                        <h5 class="modal-title" id="exampleModalLongTitle">
                            <strong><font size="5">Add To PO</font></strong>
                        </h5>
                        @*<button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                <span aria-hidden="true">&times;</span>
                            </button>*@
                    </div>
                </div>
            </div>
            <div class="modal-body">
                <form method="POST">
                    <div class="form-group">
                        <input type="hidden" id="createNewPOItemModalID" name="createNewPOItemModalName" class="form-control" value="0" />
                    </div>
                    <div class="form-group">
                        <label for="Category">Category</label>
                        @if (ViewBag.ListofCategory != null)
                        {
                            <select for="Category" class="form-control" id="Category" asp-items="@(new SelectList(ViewBag.ListofCategory, "Category", "Category"))"></select>
                        }
                    </div>
                    <div class="form-group">
                        <label for="ItemNumber">ItemName</label>
                        <select for="ItemNumber" class="form-control" id="ItemNumber" name="itemNumber" asp-items="@(new SelectList(" "))"></select>
                    </div>
                    <div class="form-group">
                        <label for="Quantity">Quantity</label>
                        <input type="text" id="Quantity" name="quantity" class="form-control" value="" />
                    </div>
                    <div class="form-group">
                        <label for="Supplier">Supplier</label>
                        <div id="Supplier"></div>
                    </div>
                    <div align="center">
                        <button class="btn" data-dismiss="modal" aria-hidden="true" style="width:80px;height:30px;border:2px; background-color:#EEEEEE"><font size="3" color="black">Close</font></button>
                        <button type="submit" class="btn btn-primary" style="width:120px;height:30px;border:2px; background-color:#3c8dbc"><font size="3" color="white">Save Changes</font></button>
                    </div>
                </form>

            </div>
        </div>
    </div>
</div>


<!-- Modal for edit quantity-->
<div class="modal fade" id="POItemModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLongTitle">Update PO Item</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form method="POST">
                    <div class="form-group">
                        <input type="hidden" id="POItemModalID" name="POItemModalName" class="form-control" value="0" />
                    </div>
                    <div class="form-group">
                        <input type="hidden" id="RowID" name="rowID" class="form-control" value="" />
                    </div>
                    <div class="form-group">
                        <label for="ItemName">Item Name</label>
                        <input type="hidden" id="ItemNamerValue" name="itemName" class="form-control" value="" />
                        <input type="text" id="ItemNameDisplay" name="" class="form-control" value="" disabled />
                    </div>
                    <div class="form-group">
                        <label for="Quantity">Quantity</label>
                        <input type="text" id="Quantity" name="quantity" class="form-control" value="" />
                    </div>
                    <div class="form-group">
                        <label for="Remark">Remark</label>
                        <input type="text" id="Remark" name="remark" class="form-control" value="" />
                    </div>
                    <div class="modal-footer">
                        <button class="btn" data-dismiss="modal" aria-hidden="true">Close</button>
                        <button type="submit" class="btn btn-primary">Save changes</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>


<script>
    $(".createNewPOItem").click(function () {
        //var $row = $(this).closest("tr");    // Find the row
        var itemNumber = ""; // Find the text
        var quantity = "";
        var remark = "";
        var createNewPOItemModalID = 1;

        var $modal = $('#createNewPOItemModal');
        $modal.find('#ItemNumber').val(itemNumber);
        $modal.find('#Quantity').val(quantity);
        $modal.find('#Remark').val(remark);
        $modal.find('#createNewPOItemModalID').val(createNewPOItemModalID);

        $modal.modal('show');
    });
</script>

<script>
    $(document).ready(function () {
        $("#Category").change(function () {
            $.get("/RaisePurchaseOrder/GetItemNameList", { category: $("#Category").val() }, function (data) {
                $("#ItemNumber").empty();
                $.each(data, function (index, row) {
                    $("#ItemNumber").append("<option value='" + row.itemNumber + "'>" + row.itemName + "</option>")
                });
            });
        })
    });
</script>

<script>
    $(document).ready(function () {
        $("#ItemNumber").change(function () {
            $.get("/RaisePurchaseOrder/GetSuppliers", { itemNumber: $("#ItemNumber").val() }, function (data) {
                $("#Supplier").empty();
                $.each(data, function (index, row) {
                    $('#Supplier').append('<input type="radio" name="supplier" value="' + row.supplierCode + '"/>' + row.supplierName + '<br/>');
                    console.log(row.supplierCode);
                });
            });
        })
    });
</script>

<script>
    $(".editPOItem").click(function () {
        var $row = $(this).closest("tr");    // Find the row
        var rowID = $row.find("td.RowID").text().trim();
        var itemName = $row.find("td.ItemName").text().trim(); // Find the text
        var quantity = $row.find("td.Quantity").text().trim();
        var remark = $row.find("td.Remark").text().trim();
        var POItemModalID = 1;

        var $modal = $('#POItemModal');
        $modal.find('#RowID').val(rowID);
        $modal.find('#ItemNameValue').val(itemName);
        $modal.find('#ItemNameDisplay').val(itemName);
        $modal.find('#Quantity').val(quantity);
        $modal.find('#Remark').val(remark);
        $modal.find('#POItemModalID').val(POItemModalID);

        $modal.modal('show');
    });
</script>



<script>
    $(document).ready(function () {
        $(".submit").click(function () {
            var itemToAdd = $("input:checkbox:checked", "#TempPurchaseOrderDetails").map(function () {
                return $(this).val();
            }).get();
            //alert(itemToAdd.join(','));
            $.ajax({
                type: "POST",
                url: '@Url.Action("Index", "RaisePurchaseOrder")',
                dataType: "html",
                data: { itemSubmitted: itemToAdd },
                success:
                    function (data) {
                        //$('#result').html(data);
                        alert("submitted");
                        window.location.reload();
                    }
            })
        });
    });
</script>


<script>
    $(".addToDraft").click(function () {
        var itemToAdd = $("input:checkbox:checked", "#TempPurchaseOrderDetails").map(function () {
            return $(this).val();
        }).get();
        alert(itemToAdd.join(','));
        $.ajax({
            type: "POST",
            url: '@Url.Action("Index", "RaisePurchaseOrder")',
            dataType: "html",
            data: { itemSavedToDraft: itemToAdd },
            success:
                function (data) {
                    //$('#result').html(data);
                    alert("saved to draft");
                    location.reload();
                }
        })
    });
</script>
